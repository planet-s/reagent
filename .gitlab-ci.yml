# Section generated by <reagent>

image: rustlang/rust:nightly

format:
    script:
        - rustup component add rustfmt
        - cargo fmt --all -- --check

linux:
    script:
        - cargo build
        - cargo test

linux:stable:
    image: rust:latest
    script:
        - cargo build
        - cargo test

redox:
    script:
        - apt-get update -qq
        - apt-get install -qq tar
        - wget -O - https://static.redox-os.org/toolchain/x86_64-unknown-redox/relibc-install.tar.gz |
          tar --extract --gzip --directory /usr/local
        - rustup target add x86_64-unknown-redox
        - env CARGO_TARGET_X86_64_UNKNOWN_REDOX_LINKER=x86_64-unknown-redox-gcc
          cargo build --target x86_64-unknown-redox

# </reagent>
