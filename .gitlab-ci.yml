# Section generated by <reagent>

image: rustlang/rust:nightly

format:
    script:
        - rustup component add rustfmt
        - cargo fmt --all -- --check

linux:
    script:
        - cargo build
        - cargo test

linux:stable:
    image: rust:latest
    script:
        - cargo build
        - cargo test



# </reagent>
